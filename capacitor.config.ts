import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scheidelholdings.leaveherjohnny',
  appName: 'Leave Her Johnny',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
